spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/autoblog
    username: autoblog
    password: autoblog
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: false

autoblog:
  security:
    jwt-secret: local-development-secret-change-before-production-please
    jwt-ttl-minutes: 60
