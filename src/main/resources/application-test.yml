spring:
  datasource:
    url: jdbc:h2:mem:autoblog;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: false

autoblog:
  storage:
    local-root: ./target/test-uploads
